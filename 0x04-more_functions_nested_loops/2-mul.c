#include "main.h"
#include <stdio.h>
/**
 * mul - Multiplies two integers and returns the results
 * Return: product of two numbers
 * @a: an integer
 * @b: an integer
 */
int mul(int a, int b)
{
return (a * b);
}
