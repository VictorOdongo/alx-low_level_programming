#include "main.h"
/**
 * _abs - Computes the absolute value of an integer
 * @x: parameter
 * Return: absolute value of a number
 */
int _abs(int x)
{
if (x < 0)
{
return (-x);
}
else
{
return (x);
}
}
